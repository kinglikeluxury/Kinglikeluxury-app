Update the Real Estate Consultation Booking System with the following requirements:

1. Consultation schedule:

* Use Georgia timezone (Asia/Tbilisi).
* Available consultation times must start from 12:00 PM and end at 8:00 PM Georgia time.
* Each consultation slot duration = 30 minutes maximum.
* Prevent overlapping bookings.
* Automatically generate available slots every 30 minutes.

2. Admin controls:

* Admin must be able to:

  * enable/disable available dates
  * block specific times
  * manually add/remove available slots
  * approve or reject consultation requests
  * see all pending requests in admin dashboard

3. User form requirements:

* Email field must be REQUIRED.
* Phone number must remain REQUIRED.
* User must accept communication/privacy terms before submitting.
* Keep form premium, clean, mobile-friendly, and multilingual-ready.

4. Email flow:
   After user submits the consultation request:

* Send automatic email:
  Subject example:
  “Your consultation request has been received”
* Email should politely inform the client that the request is under review and the team will confirm shortly.

After admin approves the booking:

* Send second confirmation email including:

  * confirmed date
  * confirmed Georgia time
  * consultation type
  * optional meeting/contact details

5. AI integration:

* If the booking form is opened from AI chat:

  * attach AI conversation summary to admin request
  * auto-fill known client preferences if available

6. UX improvements:

* Show available times only after selecting the date.
* Hide unavailable slots automatically.
* Prevent selecting past times.
* Show success confirmation screen after submission.

7. Security and scope:

* Do NOT modify production database structure unless necessary.
* Do NOT touch deployment settings, Neon configs, Railway configs, authentication system, or payment systems.
* Changes must remain isolated to consultation scheduling + email flow only.

8. Important business logic:

* The consultation request is NOT automatically confirmed.
* Final booking becomes confirmed only after admin approval.
* Users must clearly understand that the request is pending review until confirmation email is sent.
