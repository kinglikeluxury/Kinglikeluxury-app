Investigate Meta Pull Sync Post-Creation Hooks

Problem:
After fixing META_ACCESS_TOKEN, Sync Meta Leads Now imported 2 new Meta leads into CRM:
- Faruk Dewani
- Alaa Kassabry

They appear in CRM with source = meta_ads and valid emails.

But after import:
1. No Email Nurturing was created for them.
2. They were not assigned sequentially to sub-agents.
3. Need to verify whether Developer Registration records were created.

This means the Meta Pull Sync may be inserting CRM leads but not running the same post-creation hooks as manual lead creation.

Investigate only. Do not modify code yet.

Check:

1. Meta Pull Sync creation path
Find the exact function and endpoint used by Sync Meta Leads Now:
- pullSyncFromMeta()
- any insert into crm_leads
- any queue processor involved

2. Compare hooks between Manual CRM creation and Meta Pull Sync

Manual creation should trigger:
- Email Nurturing
- Sub-agent assignment / round-robin assignment
- Developer Registration records
- any lead scoring / AI processing if applicable

Meta Pull Sync should trigger the same hooks.

Compare:
A) Manual CRM lead creation hook list
B) Meta Pull Sync hook list

3. For the 2 new Meta leads, check DB:

For each lead:
- crm_leads record
- assignedAgent / assignedSubAgent value
- email nurturing status row
- email history/events row
- developer registration records
- any automation/audit logs

4. Logs

Search logs for:
- Faruk
- Alaa
- EmailNurturing
- SubAgent
- round robin
- assigned
- DeveloperRegistration
- MetaLeads
- pull_sync_inserted
- init failed
- skipped

5. Determine exact missing hooks

Report whether Meta Pull Sync currently:
- creates CRM lead only
- creates CRM lead + developer registration
- creates CRM lead + email nurturing
- creates CRM lead + sub-agent assignment

6. Return:

A) Root cause
B) Exact file/function responsible
C) Which hooks are missing
D) Whether this affects only Pull Sync or also Webhook Meta leads
E) Recommended fix

Do NOT modify code yet.