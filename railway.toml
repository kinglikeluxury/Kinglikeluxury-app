[build]
builder = "nixpacks"
buildCommand = "npm install && npx playwright install-deps chromium && npx playwright install chromium && npm run build"

[deploy]
startCommand = "npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
