import { useQuery } from "@tanstack/react-query";
import { useMemo, useState } from "react";
import { AlertTriangle, CheckCircle2, ClipboardCheck, RefreshCw, ShieldAlert, ShieldCheck, History, ExternalLink, Copy } from "lucide-react";
import { Link } from "wouter";
import { apiRequest } from "@/lib/queryClient";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

type Decision = { id:number; leadId:number|null; decisionType?:string; rationale?:string; createdAt:string; payload?:any };
type Employee = { id?:number; employee_id?:number; name?:string; employee_name?:string; availability?:string };
type Control = { mode:"shadow"|"assisted"|"controlled_automation"; decisions:Decision[]; events?:any[]; employees?:Employee[]; employeeWorkflow?:Employee[]; statusIntelligence?:any[]; operationsHealth?:any; protectedLeads?:any[] };
type RescuePreview = {
  lead:{id:number;status:string;ownerId:number;ownerName:string;contactStage?:string|null};
  decision:{id:number;state:string;statusWindow?:string|null;thresholdMinutes:number;elapsedMinutes:number;why:string;fingerprint?:string};
  protection:{protected:boolean}; blockers:{id?:number;title?:string;reason?:string}[];
  lastMission:{id?:number;missionType?:string;status?:string;createdAt?:string;objective?:string}|null;
  commitments:{id?:number;action?:string;status?:string;dueAt?:string}[];
  promises:{id?:number;promiseText?:string;status?:string;dueAt?:string}[];
  target:{id:number;name:string;role:string;active:boolean;availability:string;eligible:boolean};
};
type PreviewResponse = {preview:RescuePreview;warning:string;revalidationRequired:boolean};
type RescueSettings = { no_answer_1_threshold_hours:number; no_answer_2_threshold_hours:number; max_human_rescue_attempts:number; rescue_warning_minutes:number; protected_review_after_days:number; assisted_rescue_undo_minutes:number; auto_rescue_no_answer_1_enabled:boolean; auto_rescue_no_answer_2_enabled:boolean; auto_rescue_kill_switch:boolean; auto_rescue_canary_enabled:boolean; auto_rescue_canary_employee_ids:number[]; auto_rescue_daily_limit:number; auto_rescue_per_employee_daily_limit:number; rescue_grace_minutes:number; rescue_grace_max_count:number; auto_rescue_rule_version:string; rescue_enabled:false };
type AutoHealth = {enabled:boolean;mode:string;killSwitch:boolean;canaryEnabled:boolean;canaryEmployees:number;pendingWarnings:number;ready:number;blocked:number;rejected:number;executedToday:number;lastSuccessfulCycle?:string|null;errors:number;consecutiveFailures:number;circuit:string;leaseState:string};
type Readiness = {checked:number;wouldExecute:number;wouldBlock:number;managerReview:number;noEligibleEmployee:number;protected:number;dailyLimitImpact:number;dryRun?:boolean};
type AutoHistory = {id:number;outcome:string;rejection_reason?:string|null;created_at:string;undone_at?:string|null;rule_version?:string;rule_status?:string;queue_status?:string};

const date = (v?:string|null) => v ? new Date(v).toLocaleString([], { dateStyle:"medium", timeStyle:"short" }) : "Not available";
const label = (v:any) => v === undefined || v === null || v === "" ? "Not available" : String(v);

export default function KayControlCenterPage() {
  const control = useQuery<Control>({ queryKey:["/api/admin/kay/control"], queryFn:async() => (await apiRequest("GET","/api/admin/kay/control")).json() });
  const [selected, setSelected] = useState<Decision|null>(null);
  const [preview, setPreview] = useState<PreviewResponse|null>(null);
  const [previewError, setPreviewError] = useState("");
  const [notice, setNotice] = useState("");
  const phaseD = useQuery<any>({queryKey:["/api/admin/kay/settings/phase-d"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/settings/phase-d")).json()});
  const ownerBrief = useQuery<any>({queryKey:["/api/admin/kay/owner-brief"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/owner-brief")).json()});
  const reviews = useQuery<any>({queryKey:["/api/admin/kay/reviews"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/reviews")).json()});
  const rescueSettings = useQuery<RescueSettings>({queryKey:["/api/admin/kay/settings/rescue"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/settings/rescue")).json()});
  const autoHealth = useQuery<AutoHealth>({queryKey:["/api/admin/kay/auto-rescue/health"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/auto-rescue/health")).json()});
  const readiness = useQuery<Readiness>({queryKey:["/api/admin/kay/auto-rescue/readiness"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/auto-rescue/readiness")).json()});
  const legacyReadiness = useQuery<any>({queryKey:["/api/admin/kay/legacy-rescue-baselines/readiness"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/legacy-rescue-baselines/readiness")).json()});
  const legacyDiagnostics = useQuery<any>({queryKey:["/api/admin/kay/legacy-rescue-baselines/diagnostics"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/legacy-rescue-baselines/diagnostics")).json()});
  const e23Diagnostics = useQuery<any>({queryKey:["/api/admin/kay/legacy-rescue-baselines/e23-diagnostics"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/legacy-rescue-baselines/e23-diagnostics")).json()});
  const scope = useQuery<any>({queryKey:["/api/admin/kay/settings/operational-scope"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/settings/operational-scope")).json()});
  const autoHistory = useQuery<{history:AutoHistory[]}>({queryKey:["/api/admin/kay/auto-rescue/history"],queryFn:async()=>(await apiRequest("GET","/api/admin/kay/auto-rescue/history")).json()});
  const [legacyPreviewState,setLegacyPreview] = useState<any>(null);
  const [legacyPreviewLoading,setLegacyPreviewLoading] = useState(false);
  const mode = "recommendation_only";
  const decisions = useMemo(() => (control.data?.decisions ?? []).filter(d => d.payload?.state === "ACTIVE"), [control.data?.decisions]);
  const loadPreview = async (decision:Decision) => {
    if (!decision.leadId) return;
    setSelected(decision); setPreview(null); setPreviewError("");
    try { setPreview(await (await apiRequest("GET", `/api/admin/kay/rescue/${decision.leadId}/${decision.id}/preview`)).json() as PreviewResponse); }
    catch (e:any) { setPreviewError(e.message || "Unable to load the live rescue preview."); }
  };
  const loadLegacyPreview = async () => {
    setLegacyPreviewLoading(true);
    try { setLegacyPreview(await (await apiRequest("GET","/api/admin/kay/legacy-rescue-baselines/preview")).json()); }
    catch (e:any) { setNotice(e.message || "Unable to load the observation preview."); }
    finally { setLegacyPreviewLoading(false); }
  };

  if (control.isLoading) return <main className="min-h-[100dvh] bg-[#f4f7f6] p-6"><div className="mx-auto max-w-6xl space-y-4 animate-pulse"><div className="h-28 rounded-2xl bg-[#dbe7e3]"/><div className="h-48 rounded-2xl bg-[#e7efec]"/><div className="h-64 rounded-2xl bg-[#e7efec]"/></div></main>;
  if (control.isError) return <main className="min-h-[100dvh] bg-[#f4f7f6] p-8"><Card className="mx-auto max-w-lg border-red-200"><CardContent className="space-y-4 p-6"><ShieldAlert className="text-red-700"/><h1 className="text-xl font-semibold text-[#163b3b]">Kay Control Center unavailable</h1><p className="text-sm text-slate-600">The control snapshot could not be loaded. No CRM action was taken.</p><Button onClick={() => control.refetch()}>Retry</Button></CardContent></Card></main>;
  return <main className="min-h-[100dvh] bg-[#f4f7f6] pb-16 text-[#193b3a]">
     <header className="border-b border-[#bed2cc] bg-[#173f3d] text-[#f1f7f4]"><div className="mx-auto flex max-w-6xl items-center justify-between gap-4 px-5 py-7"><div><p className="text-xs font-semibold uppercase tracking-[.22em] text-[#a9d5ca]">Kay · Permanent CRM boundary</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Recommendation Center</h1><p className="mt-2 max-w-2xl text-sm text-[#c4dcd5]">Kay may observe, analyze, and recommend. Kay never changes CRM data; an authorized human must use the normal CRM interface.</p></div><Button variant="outline" className="border-[#709f96] bg-transparent text-white hover:bg-[#285650]" onClick={() => control.refetch()} disabled={control.isFetching}><RefreshCw className="mr-2 h-4 w-4"/>Refresh</Button></div></header>
    <div className="mx-auto max-w-6xl space-y-5 px-5 py-6">
       <Card className="border-[#a9c9c0] bg-[#fbfdfc]"><CardContent className="flex flex-wrap items-center justify-between gap-5 p-5"><div><p className="text-xs font-bold uppercase tracking-widest text-[#57756e]">Permanent operating mode</p><div className="mt-2 flex flex-wrap items-center gap-3"><Badge className="bg-[#d8eee8] text-[#205c52] hover:bg-[#d8eee8]">{mode.replaceAll("_"," ").toUpperCase()}</Badge><span className="text-sm font-medium">Read-only recommendations</span></div></div><p className="max-w-xl text-sm leading-6 text-slate-600">This UI cannot execute Rescue, undo a historical execution, change an owner, or change a CRM status. Recommendation history remains visible for audit; CRM actions belong to authorized humans in the normal CRM.</p></CardContent></Card>
       <Card className="border-[#d6b36a] bg-[#fffdf7]"><CardContent className="flex items-start gap-3 p-5 text-sm"><ShieldAlert className="mt-0.5 h-5 w-5 shrink-0 text-[#a66b20]"/><div><p className="font-semibold text-[#765016]">KAY CRM ACCESS: READ ONLY</p><p className="mt-1 text-slate-700">Kay can explain a recommendation and show the current CRM values, but it cannot perform the recommended change. Open the Lead to let an authorized employee manually review and act. This permanent rule is independent of mode, approval, kill switch, canary, scheduler, or endpoint.</p></div></CardContent></Card>
      {notice && <div role="status" className="rounded-xl border border-[#9ccabd] bg-[#e7f5ef] p-4 text-sm font-medium text-[#205c52]"><CheckCircle2 className="mr-2 inline h-4 w-4"/>{notice}</div>}
      <section><div className="mb-3 flex items-end justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-[#57756e]">Decision ledger</p><h2 className="mt-1 text-2xl font-semibold">Active rescue recommendations</h2></div><Badge variant="outline">{decisions.length} active</Badge></div>
        {decisions.length === 0 ? <Card><CardContent className="p-8 text-center text-sm text-slate-600">No active valid rescue decisions require review.</CardContent></Card> :
        <div className="space-y-3">{decisions.map(d => <DecisionRow key={d.id} decision={d} onReview={() => loadPreview(d)}/>)}</div>}
      </section>
      <Card className="border-[#bed2cc]"><CardHeader><CardTitle className="flex items-center gap-2 text-lg"><ClipboardCheck className="h-5 w-5 text-[#287567]"/>Operational trace</CardTitle></CardHeader><CardContent className="grid gap-3 text-sm sm:grid-cols-3"><Metric title="Scheduler" value={label(control.data?.operationsHealth?.scheduler ?? "Disabled")}/><Metric title="Last successful cycle" value={date(control.data?.operationsHealth?.lastSuccessfulCycle)}/><Metric title="Protected leads" value={label(control.data?.protectedLeads?.length ?? 0)}/></CardContent></Card>
         <LegacyBaselineCard readiness={legacyReadiness.data} preview={legacyPreviewState} onPreview={loadLegacyPreview} busy={legacyPreviewLoading}/>
        <LegacyDiagnosticsCard data={legacyDiagnostics.data}/>
         <E23DiagnosticsCard data={e23Diagnostics.data}/>
         <OperationalScopeCard data={scope.data} loading={scope.isLoading} error={scope.isError} />
          <E2ControlPanel settings={rescueSettings.data} health={autoHealth.data} readiness={readiness.data} history={autoHistory.data?.history??[]}/>
         <PhaseDCard data={phaseD.data} ownerText={ownerBrief.data?.text}/>
       <ReviewQueue reviews={reviews.data?.reviews??[]}/>
      <LegacyIntelligence data={control.data}/>
    </div>
    {selected && <RescueReview decision={selected} preview={preview} error={previewError} onCopy={(text:string) => { const copyPromise = navigator.clipboard?.writeText(text); if (copyPromise) copyPromise.then(() => setNotice("Recommendation copied. No CRM change was made."), () => setNotice("Recommendation could not be copied; no CRM change was made.")); else setNotice("Clipboard unavailable; no CRM change was made."); }} onClose={() => setSelected(null)} />}
  </main>;
}
function OperationalScopeCard({data,loading,error}:{data:any;loading:boolean;error:boolean}) {
  if(loading)return <Card><CardContent className="p-5 text-sm text-slate-600">Loading operational scope configuration…</CardContent></Card>;
  if(error||!data)return <Card className="border-red-200"><CardContent className="p-5 text-sm text-red-700">Operational scope unavailable. Kay remains fail-closed; no setting was changed.</CardContent></Card>;
  const config=data.config;
  return <Card className="border-[#9fbfb5]"><CardHeader><CardTitle>Kay monitoring scope · read only</CardTitle></CardHeader><CardContent className="space-y-4 text-sm"><p className="text-slate-600">Fixed cohort, not a rolling 90-day window. Configuration status: <b>{data.status}</b>. Scope settings are frozen while Kay is recommendation-only.</p>{config&&<div className="grid gap-2 sm:grid-cols-3"><Metric title="Launch" value={config.launchAtIso}/><Metric title="Fixed cutoff" value={config.cutoffAtIso}/><Metric title="Timezone" value={config.timezone}/></div>}<div><b>Append-only audit history</b><div className="mt-2 max-h-48 space-y-2 overflow-auto">{(data.audit??[]).length?(data.audit.map((a:any)=><div className="rounded border p-2 text-xs" key={a.id}><b>{a.actor}</b> · {a.createdAt}<br/>Launch {String(a.launchAt)} · cutoff {String(a.cutoffAt)} · {a.timezone}<br/>Old {JSON.stringify(a.oldValue)} → New {JSON.stringify(a.newValue)}</div>)):<p className="mt-2 text-xs text-slate-500">No audit entries yet.</p>}</div></div></CardContent></Card>;
}
function LegacyBaselineCard({readiness,preview,onPreview,busy}:{readiness:any;preview:any;onPreview:()=>void;busy:boolean}) {
  const statuses=readiness?.statuses??{};
  return <Card className="border-[#d6b36a] bg-[#fffdf7]"><CardHeader><CardTitle>Legacy Rescue Baseline · observation only</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><p className="text-slate-600">Actual historical status-entry time is unknown. Historical evidence remains visible, but Kay cannot initialize records, change ownership, or change CRM status.</p><div className="grid gap-2 sm:grid-cols-2">{["no_answer_1","no_answer_2"].map(s=><div className="rounded border p-3" key={s}><b>{s}</b><div className="mt-2 grid grid-cols-2 gap-1 text-xs">{Object.entries(statuses[s]??{}).map(([k,v])=><p key={k}><span className="text-slate-500">{k}:</span> <b>{String(v)}</b></p>)}</div></div>)}</div><div className="flex flex-wrap items-center gap-2"><Button variant="outline" onClick={onPreview} disabled={busy}>Preview observation</Button><Badge variant="outline">Initialization frozen</Badge></div>{preview&&<p className="text-xs text-slate-600">Inspected {preview.inspected}; eligible {preview.eligible}. Preview only; no ownership, status, or internal record was written.</p>}</CardContent></Card>;
}
function E23DiagnosticsCard({data}:{data:any}) {
  if (!data) return <Card className="border-red-200 bg-white"><CardHeader><CardTitle>E.2.3 diagnostics unavailable</CardTitle></CardHeader><CardContent className="text-sm text-slate-600">Monitoring scope, ownership, and readiness are unavailable. No Kay action was taken.</CardContent></Card>;
  const policy=data?.ownershipPolicy?.kinglike_admin;
  const scope=data.monitoringScope;
  return <Card className="border-[#d6b36a] bg-[#fffdf7]"><CardHeader><CardTitle>Rescue ownership policy · E.2.3</CardTitle></CardHeader><CardContent className="space-y-4 text-sm"><p className="text-slate-600">Read-only policy and capacity simulation. No production routing formula or ownership is changed.</p>{scope&&<><p className="rounded border border-[#d6b36a] bg-[#fff9e8] p-3 text-xs">Kay manages the fixed three-month pre-launch cohort plus all future Leads. This is not a rolling 90-day window.<br/>Launch: <b>{scope.launchAt}</b> · Fixed cutoff: <b>{scope.fixedCutoffAt}</b></p><div className="grid gap-2 sm:grid-cols-5">{[["All CRM",scope.all_crm],["In scope",scope.in_scope],["Out legacy",scope.out_legacy],["Excluded admin",scope.excluded_admin],["Date uncertain",scope.uncertain]].map(([k,v])=><Metric key={String(k)} title={String(k)} value={String(v)}/>)}</div></>}<div className="grid gap-2 sm:grid-cols-3"><Metric title="kinglike_admin classification" value={policy?.classification??"Unavailable"}/><Metric title="Can receive Rescue" value={policy?.canReceiveRescue===false?"NO":"Unavailable"}/><Metric title="Can be rescued from" value={policy?.canBeRescuedFrom===false?"NO":"Unavailable"}/></div>{data.noAnswerScope&&<div className="rounded border p-3"><b>No-answer scope partition</b><div className="mt-2 grid grid-cols-2 gap-2 text-xs sm:grid-cols-4">{Object.entries(data.noAnswerScope).map(([k,v])=><Metric key={k} title={k} value={String(v)}/>)}</div><p className="mt-2 text-xs text-slate-500">Readiness calculations use managed/in-scope leads only.</p></div>}{data.employeeOwnership&&<div className="overflow-x-auto"><b>Employee ownership (aggregate)</b><table className="mt-2 w-full text-xs"><thead><tr><th className="text-left">Employee</th><th>CRM total</th><th>In scope</th><th>Legacy</th><th>Uncertain</th></tr></thead><tbody>{data.employeeOwnership.map((x:any)=><tr key={x.employee}><td>{x.employee}</td><td className="text-center">{x.totalCrm}</td><td className="text-center">{x.inScope}</td><td className="text-center">{x.outOfScopeLegacy}</td><td className="text-center">{x.uncertain}</td></tr>)}</tbody></table></div>}{data?.recommendedCapacity&&<p className="text-xs text-slate-600">Recommended capacity (inactive): {data.recommendedCapacity.formula}</p>}<p className="text-xs text-slate-500">Safety: MODE={data.safety?.mode ?? "Unavailable"} · Kill switch={data.safety?.killSwitch ?? "Unavailable"} · Canary employees={data.safety?.canaryEmployeeCount ?? "Unavailable"}</p></CardContent></Card>;
  const simulation=data.routingSimulation;
  return <Card className="border-[#d6b36a] bg-[#fffdf7]"><CardHeader><CardTitle>Rescue ownership policy · E.2.3</CardTitle></CardHeader><CardContent className="space-y-4 text-sm"><p className="text-slate-600">Read-only policy and capacity simulation. No production routing formula or ownership is changed.</p>{scope&&<><p className="rounded border border-[#d6b36a] bg-[#fff9e8] p-3 text-xs">Kay manages the fixed three-month pre-launch cohort plus all future Leads. This is not a rolling 90-day window.<br/>Launch: <b>{scope.launchAt}</b> · Fixed cutoff: <b>{scope.fixedCutoffAt}</b></p><div className="grid gap-2 sm:grid-cols-5">{[["All CRM",scope.all_crm],["In scope",scope.in_scope],["Out legacy",scope.out_legacy],["Excluded admin",scope.excluded_admin],["Date uncertain",scope.uncertain]].map(([k,v])=><Metric key={String(k)} title={String(k)} value={String(v)}/>)}</div></>}<div className="grid gap-2 sm:grid-cols-3"><Metric title="kinglike_admin classification" value={policy?.classification??"Unavailable"}/><Metric title="Can receive Rescue" value={policy?.canReceiveRescue===false?"NO":"Unavailable"}/><Metric title="Can be rescued from" value={policy?.canBeRescuedFrom===false?"NO":"Unavailable"}/></div>{data.noAnswerScope&&<div className="rounded border p-3"><b>No-answer scope partition</b><div className="mt-2 grid grid-cols-2 gap-2 text-xs sm:grid-cols-4">{Object.entries(data.noAnswerScope).map(([k,v])=><Metric key={k} title={k} value={String(v)}/>)}</div><p className="mt-2 text-xs text-slate-500">Readiness calculations use managed/in-scope leads only.</p></div>}{data.employeeOwnership&&<div className="overflow-x-auto"><b>Employee ownership (aggregate)</b><table className="mt-2 w-full text-xs"><thead><tr><th className="text-left">Employee</th><th>CRM total</th><th>In scope</th><th>Legacy</th><th>Uncertain</th></tr></thead><tbody>{data.employeeOwnership.map((x:any)=><tr key={x.employee}><td>{x.employee}</td><td className="text-center">{x.totalCrm}</td><td className="text-center">{x.inScope}</td><td className="text-center">{x.outOfScopeLegacy}</td><td className="text-center">{x.uncertain}</td></tr>)}</tbody></table></div>}{simulation&&<div className="rounded border p-3"><b>10 hypothetical NEW Rescue leads · no database writes</b><p className="mt-1 text-xs">Assigned {simulation.assigned}/{simulation.requested}; deferred {simulation.deferred}; concentration: <b>{simulation.concentrationRisk}</b> (threshold {simulation.concentrationThreshold}). {simulation.concentrationExplanation}</p>{simulation.deferredReasons?.length>0&&<p className="mt-1 text-xs text-amber-700">Deferred reasons: {simulation.deferredReasons.join(", ")}</p>}<div className="mt-2 overflow-x-auto"><table className="w-full text-xs"><thead><tr><th className="text-left">Employee</th><th>Starting capacity</th><th>Projected capacity</th><th>Would receive</th></tr></thead><tbody>{Object.keys(simulation.startingCapacity??{}).map((name:string)=><tr key={name}><td>{name}</td><td>{simulation.startingCapacity[name]}</td><td>{simulation.projectedCapacity?.[name]}</td><td>{simulation.receivingByEmployee?.[name] ?? "—"}</td></tr>)}</tbody></table></div></div>}{data?.recommendedCapacity&&<p className="text-xs text-slate-600">Recommended capacity (inactive diagnostic): {data.recommendedCapacity.formula}; production automation remains fail-closed unless an approved scoped formula exists.</p>}<p className="text-xs text-slate-500">Safety: MODE={data.safety?.mode ?? "Unavailable"} · Kill switch={data.safety?.killSwitch ?? "Unavailable"} · Canary employees={data.safety?.canaryEmployeeCount ?? "Unavailable"}</p></CardContent></Card>;
}
function LegacyDiagnosticsCard({data}:{data:any}) {
  return <Card><CardHeader><CardTitle>Legacy owner & capacity diagnostics</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><p className="text-slate-600">{data?.recommendation??"Loading read-only diagnostics…"}</p><div className="grid gap-2 sm:grid-cols-2">{(data?.owners??[]).map((o:any)=><div className="rounded border p-2" key={o.account}><b>{o.account}</b><span className="ml-2 text-xs">{o.classification}</span><p>Active {o.active_leads} · NA1 {o.no_answer_1} · NA2 {o.no_answer_2}</p><p className="text-xs text-slate-500">{o.reason}</p>{o.conclusion&&<p className="text-xs font-semibold">{o.conclusion}</p>}<ul className="list-disc pl-4 text-xs">{(o.evidence??[]).map((e:string)=><li key={e}>{e}</li>)}</ul><p className="text-xs">Status mix: {(o.statusMix??[]).map((s:any)=>`${s.status} ${s.count}`).join(" · ")}</p></div>)}</div>{data?.ageBuckets&&<p className="text-xs text-slate-600">Age buckets: {Object.entries(data.ageBuckets).map(([k,v])=>`${k}: ${v}`).join(" · ")}</p>}<div className="overflow-x-auto"><table className="w-full text-xs"><thead><tr><th>Employee</th><th>All nonterminal</th><th>Touched 30</th><th>Touched 60</th><th>Touched 90</th></tr></thead><tbody>{(data?.capacitySensitivity??[]).map((r:any)=><tr key={r.employee}><td>{r.employee}</td><td>{r.all_nonterminal}</td><td>{r.touched30}</td><td>{r.touched60}</td><td>{r.touched90}</td></tr>)}</tbody></table></div></CardContent></Card>;
}
function E2ControlPanel({settings,health,readiness,history}:{settings?:RescueSettings;health?:AutoHealth;readiness?:Readiness;history:AutoHistory[]}) {
  if (!settings) return <Card><CardContent className="p-5 text-sm">Loading automatic-rescue history…</CardContent></Card>;
  return <section className="overflow-hidden rounded-2xl border border-[#d6b36a] bg-[#fffdf7] shadow-sm"><div className="border-b border-[#ead5ad] bg-[#fff8e8] p-5"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#765016]">Phase E.2 · permanently frozen</p><div className="mt-1 flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-2xl font-semibold">Rescue recommendations and history</h2><p className="mt-1 text-sm text-[#765016]">Detection, warnings, target reasoning, and historical execution evidence remain visible. New CRM execution is unavailable.</p></div><Badge className="bg-[#f5e3b7] text-[#765016] hover:bg-[#f5e3b7]">FROZEN_NO_EXECUTION</Badge></div><p className="mt-3 text-sm text-slate-700">Kay cannot start a worker cycle, change safeguards, transfer a Lead, change status, or undo an execution. An authorized human can open the Lead in the normal CRM and act under existing permissions.</p></div><div className="space-y-4 p-5"><div className="grid gap-3 sm:grid-cols-3"><Metric title="Mode" value="Recommendation only"/><Metric title="Scheduler" value="Disabled by policy"/><Metric title="CRM mutation by Kay" value="Denied"/></div><div className="rounded-xl border border-[#d6b36a] bg-white p-4"><div className="flex items-center gap-2"><ShieldCheck className="h-4 w-4 text-[#a66b20]"/><b>Frozen safety snapshot</b></div><div className="mt-3 grid grid-cols-2 gap-3 text-sm">{[["Scheduler","disabled by policy"],["Lease",health?.leaseState??"unknown"],["Warnings",health?.pendingWarnings??0],["Recommendations ready",health?.ready??0],["Blocked",health?.blocked??0],["Historical executions",health?.executedToday??0],["Failures",health?.consecutiveFailures??0],["Last cycle",date(health?.lastSuccessfulCycle)]].map(([k,v])=><div key={String(k)}><p className="text-xs text-slate-500">{k}</p><b>{String(v)}</b></div>)}</div></div><div className="grid gap-4 lg:grid-cols-2"><div className="rounded-xl border p-4"><b>Recommendation readiness</b>{readiness?<div className="mt-3 grid grid-cols-2 gap-2 text-sm">{Object.entries(readiness).filter(([k])=>k!=="dryRun").map(([k,v])=><div className="rounded bg-[#fff8e8] p-2" key={k}><span className="block text-xs text-slate-500">{k}</span><b>{String(v)}</b></div>)}</div>:<p className="mt-2 text-sm text-slate-600">Readiness unavailable.</p>}</div><div className="rounded-xl border p-4"><div className="flex items-center gap-2"><History className="h-4 w-4 text-[#a66b20]"/><b>Historical execution records</b></div><p className="mt-1 text-xs text-slate-600">Preserved for audit. They are not re-run or relabeled by this UI.</p><div className="mt-3 max-h-48 space-y-2 overflow-auto">{history.length?history.map(row=><div className="rounded border p-2 text-xs" key={row.id}><b>#{row.id} · {row.outcome}</b><span className="ml-2 text-slate-500">{date(row.created_at)}</span><p className="mt-1 text-slate-600">{row.queue_status??"—"} · {row.rule_status??"—"} · {row.rejection_reason??row.rule_version??"No exception"}{row.undone_at?` · historical undo ${date(row.undone_at)}`:""}</p></div>):<p className="text-sm text-slate-600">No automatic rescue history.</p>}</div></div></div></div></section>;
}
function DecisionRow({decision,onReview}:{decision:Decision;onReview:()=>void}) { const p=decision.payload??{}; return <Card className="border-l-4 border-l-[#d39b45]"><CardContent className="flex flex-wrap items-center justify-between gap-4 p-5"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className="font-semibold">Lead {decision.leadId ?? "—"}</span><Badge className="bg-[#fff1d9] text-[#825a1d] hover:bg-[#fff1d9]">{p.state ?? "ACTIVE"}</Badge><span className="text-xs text-slate-500">{date(decision.createdAt)}</span></div><p className="mt-2 text-sm text-slate-600">{p.employee_selection_explanation ?? decision.rationale ?? "Kay identified a rescue review candidate."}</p></div>{decision.leadId ? <div className="flex flex-wrap gap-2"><Button onClick={onReview} className="bg-[#17685d] text-white hover:bg-[#10554b]">View recommendation</Button><Button asChild variant="outline"><Link href={`/admin/crm/${decision.leadId}`}>Open Lead</Link></Button></div> : <Badge variant="outline" className="border-[#d39b45] text-[#825a1d]">No Lead link available</Badge>}</CardContent></Card>; }
function Metric({title,value}:{title:string;value:string}) { return <div className="rounded-lg bg-[#eef5f2] p-3"><p className="text-xs uppercase tracking-wide text-slate-500">{title}</p><p className="mt-1 font-semibold">{value}</p></div>; }
function PhaseDCard({data,ownerText}:{data:any;ownerText?:string}) {
  const phaseDValue=data??{};
  const speak=()=>{if(!ownerText||!("speechSynthesis" in window))return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(ownerText);u.lang=phaseDValue.default_language==="ar"?"ar-SA":"en-US";u.rate=Number(phaseDValue.speech_rate??1);u.pitch=Number(phaseDValue.speech_pitch??1);const voice=window.speechSynthesis.getVoices().find(v=>v.name===phaseDValue.preferred_voice_name);if(voice)u.voice=voice;window.speechSynthesis.speak(u);};
  // Canonical preferred_voice_name and employee profile defaults remain visible in
  // historical data, but the settings writer is intentionally frozen:
  // k === "preferred_voice_name" ? (e.target.value.trim() || null)
  // address: String(employee?.name ?? `Employee ${e.target.value}`)
  return <Card className="border-[#9fbfb5]"><CardHeader><CardTitle>Phase D · internal briefing snapshot</CardTitle></CardHeader><CardContent className="space-y-4 text-sm"><p className="text-slate-600">Kay internal briefing data is shown for observation. Current internal writers and evaluators are frozen; no CRM action or customer contact is initiated here.</p><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Metric title="Enabled" value={phaseDValue.enabled?"Reported enabled":"Disabled"}/><Metric title="Voice" value={phaseDValue.voice_enabled===false?"Disabled":"Browser local only"}/><Metric title="Language" value={label(phaseDValue.default_language)}/><Metric title="Brief length" value={label(phaseDValue.brief_length)}/></div><div className="rounded border bg-[#f1f6f4] p-3"><b>Owner Brief</b><p className="mt-1">{ownerText??"Loading owner brief…"}</p></div><div className="flex flex-wrap items-center gap-2"><Button variant="outline" onClick={speak} disabled={!ownerText||phaseDValue.voice_enabled===false}>Play locally</Button><Badge variant="outline">Settings and evaluator controls frozen</Badge></div></CardContent></Card>;
}
function ReviewQueue({reviews,onAction: _onAction}:{reviews:any[];onAction?:(_:number,_kind:"resolve"|"return")=>void}) { return <Card className="border-[#d5b374]"><CardHeader><CardTitle>Manager reviews · read only</CardTitle></CardHeader><CardContent className="space-y-2"><p className="text-xs text-slate-600">Review history is preserved. Kay cannot resolve, return, or otherwise mutate CRM data from this surface.</p>{reviews.length===0?<p className="text-sm text-slate-600">No manager reviews.</p>:reviews.map(r=><div className="rounded border p-3" key={r.id}><div className="flex justify-between"><b>{r.title??r.reason??`Review #${r.id}`}</b><Badge>{r.status??"OPEN"}</Badge></div><p className="my-2 text-sm text-slate-600">{r.description??r.rationale??"Manager context is required."}</p></div>)}</CardContent></Card>; }
function LegacyIntelligence({data}:{data?:Control}) { const workflow:any[]=data?.employeeWorkflow??[]; const statuses:any[]=data?.statusIntelligence??[]; return <div className="grid gap-5 lg:grid-cols-2"><Card><CardHeader><CardTitle>CRM status intelligence</CardTitle></CardHeader><CardContent className="overflow-x-auto"><table className="w-full text-xs"><thead><tr className="text-left text-slate-500"><th className="p-2">Status</th><th className="p-2">Classification</th><th className="p-2">Rescue</th><th className="p-2">Workflow meaning</th></tr></thead><tbody>{statuses.map((s:any)=><tr className="border-t" key={s.status}><td className="p-2 font-medium">{s.status}</td><td className="p-2">{s.classification}</td><td className="p-2">{s.rescueEvaluated?"Yes":"No"}</td><td className="p-2">{s.description}</td></tr>)}</tbody></table>{!statuses.length&&<p className="p-3 text-sm text-slate-600">No status intelligence records.</p>}</CardContent></Card><Card><CardHeader><CardTitle>Employee workflow intelligence</CardTitle></CardHeader><CardContent className="space-y-2">{workflow.length?workflow.map((e:any)=><div className="rounded border p-3 text-sm" key={e.employee_id}><b>{e.employee_name}</b><p className="mt-1 text-slate-600">Active {e.active_missions} · Critical {e.active_critical} · Rescue risk {e.rescue_risk} · Completed {e.completed} · Stale {e.stale}</p></div>):<p className="text-sm text-slate-600">No eligible employee workflow records.</p>}</CardContent></Card></div>; }
function RescueReview({decision,preview,error,onCopy,onClose}:{decision:Decision;preview:PreviewResponse|null;error:string;onCopy:(text:string)=>void;onClose:()=>void}) {
  const p:RescuePreview|undefined=preview?.preview;
  const recommendation=p?`Lead #${p.lead.id}: Kay recommends reviewing the owner assignment. Current owner: ${p.lead.ownerName}. Recommended target: ${p.target.name}. Reason: ${p.decision.why}`:"";
  return <div className="fixed inset-0 z-50 overflow-y-auto bg-[#173f3d]/70 p-4" role="dialog" aria-modal="true" aria-labelledby="rescue-review-title"><div className="mx-auto my-6 max-w-3xl rounded-2xl border border-[#bed2cc] bg-[#fbfdfc] shadow-2xl"><div className="flex items-start justify-between border-b border-[#d5e2de] p-5"><div><p className="text-xs font-bold uppercase tracking-widest text-[#a96825]">Recommendation · read only</p><h2 id="rescue-review-title" className="mt-1 text-2xl font-semibold">Review recommendation for Lead {decision.leadId}</h2><p className="mt-1 text-sm text-slate-600">Kay explains the recommendation and current CRM values. Kay cannot execute, undo, change the owner, or change the status.</p></div><Button variant="ghost" onClick={onClose} aria-label="Close recommendation">Close</Button></div><div className="space-y-5 p-5">{error&&<div role="alert" className="rounded-lg bg-[#fff0ed] p-3 text-sm text-[#943f32]">{error}</div>}{!p&&!error&&<div className="flex items-center gap-2 rounded-lg bg-[#eef5f2] p-4 text-sm"><RefreshCw className="h-4 w-4 animate-spin"/>Loading read-only preview…</div>}{p&&<><div className="rounded-lg border border-[#d6b36a] bg-[#fff8e8] p-4 text-sm text-[#765016]"><AlertTriangle className="mr-2 inline h-5 w-5"/>Permanent rule: Kay CRM access is read only. Any owner or status change must be performed manually by an authorized human in the normal CRM.</div><div className="grid gap-3 sm:grid-cols-2">{[["Lead",`#${p.lead.id}`],["Current owner",p.lead.ownerName],["Recommended target",p.target.name],["Current status",p.lead.status],["Contact stage",label(p.lead.contactStage)],["Status window",date(p.decision.statusWindow)],["Time / threshold",`${p.decision.elapsedMinutes} min / ${p.decision.thresholdMinutes} min`],["Protected",p.protection.protected?"Yes":"No"],["Target eligibility",p.target.eligible?`${p.target.role} · ${p.target.availability}`:"Not eligible"],["Fingerprint",label(p.decision.fingerprint)]].map(([a,b])=><div key={a} className="rounded-lg border border-[#d5e2de] bg-white p-3"><p className="text-xs uppercase tracking-wide text-slate-500">{a}</p><p className="mt-1 break-words text-sm font-semibold">{b}</p></div>)}</div><div className="rounded-lg bg-[#eef5f2] p-4 text-sm"><p className="font-semibold">WHY this recommendation exists</p><p className="mt-1 text-slate-700">{p.decision.why}</p></div><LiveList title="Blockers" items={p.blockers} empty="No live blockers."/><LiveList title="Last mission" items={p.lastMission?[p.lastMission]:[]} empty="No prior mission supplied."/><LiveList title="Open commitments" items={p.commitments} empty="No open commitments."/><LiveList title="Open promises" items={p.promises} empty="No open promises."/><div className="flex flex-wrap justify-end gap-2 border-t border-[#d5e2de] pt-4"><Button asChild className="bg-[#17685d] hover:bg-[#10554b]"><Link href={`/admin/crm/${p.lead.id}`}>Open Lead in CRM <ExternalLink className="ml-2 h-4 w-4"/></Link></Button><Button variant="outline" onClick={()=>onCopy(recommendation)}><Copy className="mr-2 h-4 w-4"/>Copy recommendation</Button><Button variant="ghost" onClick={onClose}>Close review</Button></div></>}</div></div></div>;
}
function LiveList({title,items,empty}:{title:string;items:Record<string,any>[];empty:string}) { return <div className="rounded-lg border border-[#d5e2de] p-3 text-sm"><p className="font-semibold">{title}</p>{items.length?items.map((item,index)=><pre className="mt-2 overflow-x-auto rounded bg-[#f1f6f4] p-2 text-xs whitespace-pre-wrap" key={item.id??index}>{JSON.stringify(item,null,2)}</pre>):<p className="mt-1 text-slate-600">{empty}</p>}</div>; }